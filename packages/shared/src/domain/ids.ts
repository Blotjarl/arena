/** Stable identifier for a Player row. */
export type PlayerId = string;
/** Stable identifier for a Match instance. */
export type MatchId = string;
/** Identifier for a fixed roster entry in `ChampionRoster` (e.g. `'korr'`, `'vex'`, `'rin'`). */
export type ChampionId = string;
